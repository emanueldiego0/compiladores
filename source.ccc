program nome_programa;
var
	a : integer;
	b : integer;
	c : integer;
	d : real;
begin
	a := a + b;
	a := a - b;
	a := a * b;
	a := a / b;

	if(a > b) then
		c := a;
	else if(a <> c) then
		c := a;

	while((a + 1) >= b) do
	begin
		a := a + 1;
	end

	writeln(a);
	read(a);
	writeln(a + b);
	
	if(a <> b) then
		begin
			a := a + b;
		end
	else
		begin
			a := b + c;
		end

	begin
		var
			a:integer;
			b:integer;
		writeln(a);
		read(b);
	end
end
